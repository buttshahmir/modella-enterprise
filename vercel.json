{
  "outputDirectory": "dist/public",
  "buildCommand": "npm install --prefix client && npm run vercel-build --prefix client",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}